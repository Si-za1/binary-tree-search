#include "array.h"
#include"binary search.h"
#include <iostream>
int main()
{
	ArrayBST t;
	t.add(3);
	t.add(4);
	t.add(6);
	t.add(10);
	t.preorder traversal();
	
	 for(int i=0;i<MAXSIZE;i++)
	 std::cout<<t.element[i];
}
 std::cout<<std::endl;
 a.preorder Traversal(10);
 if(t.search(6))
 {
 	std::cout<<"6 is present"<<std::endl;
 	
 } 
   else 
   {
   	std::cout<<"6 is absent"<<std::endl;
   	
   }
	
}
